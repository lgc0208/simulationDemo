# -*- mode: python ; coding: utf-8 -*-

block_cipher = None


a = Analysis(['simulationDemo.py', 'planeBattle'],
             pathex=['E:\\北京邮电大学\\创新创业项目\\大学生创新创业项目\\信道建模_光国重\\simulationDemo_python'],
             binaries=[],
             datas=[],
             hiddenimports=[],
             hookspath=[],
             runtime_hooks=[],
             excludes=[],
             win_no_prefer_redirects=False,
             win_private_assemblies=False,
             cipher=block_cipher,
             noarchive=False)
pyz = PYZ(a.pure, a.zipped_data,
             cipher=block_cipher)
exe = EXE(pyz,
          a.scripts,
          a.binaries,
          a.zipfiles,
          a.datas,
          [],
          name='simulationDemo',
          debug=False,
          bootloader_ignore_signals=False,
          strip=False,
          upx=True,
          upx_exclude=[],
          runtime_tmpdir=None,
          console=False )
